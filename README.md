
  # One Page Portfolio UI

  This is a code bundle for One Page Portfolio UI. The original project is available at https://www.figma.com/design/wSFxXdgMHkPI4ynPfXD0DG/One-Page-Portfolio-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  